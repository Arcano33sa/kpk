KSA PRÁCTIKA — Seguimiento — Etapa 2/2 — Integración final

SMOKE TEST AUTOMATIZADO: APROBADO

Validaciones ejecutadas:
- app.js: sintaxis JavaScript válida con node --check.
- Navegación y render local en Chromium headless con almacenamiento simulado: 0 errores de página y 0 errores de consola.
- Último pedido global: selecciona la Fecha OC válida más reciente por Cliente + Sucursal.
- OC anulada: ignorada sin modificar el documento.
- Compatibilidad antigua: relación de lectura por nombres exactos cuando faltan IDs.
- Semáforo: verde 0–24, amarillo 25–29, rojo 30+ y nunca llamada.
- Nueva llamada: actualiza Última llamada, reinicia días, conserva la anterior en Histórico y deja solo Seguimiento pendiente.
- Resumen: oculta líneas verdes, muestra contador Sucursales al día y ordena nunca llamada/rojo/amarillo.
- Baseline: Seguimiento recibe cursor seguro desde metadata confirmada previa sin cambiar la versión del contrato.
- JSON completo: conserva llamadas e IDs.
- Responsive: sin scroll horizontal general en desktop 1440 px, iPad 1024 px y móvil 390 px.
- Firestore: verificación estática positiva de colección, escritura parcial, lectura completa, selección incremental, aplicación, fusión, conflictos, cursores y tombstones.
- PWA: versión, referencias y nombre de caché actualizados.

Nota: no se ejecutó una operación real contra el proyecto Firestore ni Safari físico dentro del entorno de empaquetado; se validaron sus contratos, rutas y compatibilidad de código, además del smoke local en Chromium.
